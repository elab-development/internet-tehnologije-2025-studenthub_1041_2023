version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: studentska_sluzba_-mysql
    environment:
      MYSQL_DATABASE: studentska_sluzba_db
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_ROOT_HOST: "%"
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"
    volumes:
      - studentska_sluzba__mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot --silent"]
      interval: 5s
      timeout: 5s
      retries: 30

  backend:
    build:
      context: ./studentska_sluzba_be
      dockerfile: Dockerfile
    container_name: studentska_sluzba_be
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_URL: http://localhost:8000

      DB_CONNECTION: mysql
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: studentska_sluzba__db
      DB_USERNAME: root
      DB_PASSWORD: ""

      SESSION_DRIVER: database
      CACHE_STORE: database
      QUEUE_CONNECTION: database
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./studentska_sluzba_fe
      dockerfile: Dockerfile
    container_name: studentska_sluzba_fe
    depends_on:
      - backend
    environment:
      CHOKIDAR_USEPOLLING: "true"
      HOST: "0.0.0.0"
    ports:
      - "3000:3000"

volumes:
  studentska_sluzba__mysql_data: